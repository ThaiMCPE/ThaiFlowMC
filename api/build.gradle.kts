description = "Stable, engine-agnostic API surface exposed to ThaiFlowMC mods."

dependencies {
    // intentionally dependency-free: this module must never require Minecraft
    // or GraalPy on its compile classpath.
}
